gatling {
  data {
    writers = [console, file, statsd]

    statsd {
      host = "localhost"
      port = 9125
      bufferSize = 8192
      writeInterval = 1
    }
  }
}

logger {
    root = DEBUG
}